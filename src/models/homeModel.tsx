export const getWelcomeMessage = (): string => {
  return "Welcome to the REST Countries API"
}

export const getFeatures = (): string[] => {
  return [
    "Fast and efficient with Bun",
    "Modular architecture with Hono",
    "Dynamic views with HTMX",
  ]
}
